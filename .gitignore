# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build
/dist

# bundled worker/migrate/seed artifacts are built in CI/Docker
# test + tooling artifacts
/playwright-report
/test-results
/.playwright-mcp
*.png

# env / secrets (server-only, never committed)
.env
.env.*
!.env.example

# misc
.DS_Store
*.pem
npm-debug.log*

# local backups / dumps
/backups

# scratch: загруженные пользователем файлы (xlsx и т.п.) и локальные хелперы
/tmp

# артефакты импорта ставок (персональные данные участников — не коммитим)
tools/xlsx-import/bets.json
tools/xlsx-import/*.cjs
tools/xlsx-import/node_modules
