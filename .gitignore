# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build
/dist

# bundled worker/migrate/seed artifacts are built in CI/Docker
# test + tooling artifacts
/playwright-report
/test-results
/.playwright-mcp
*.png

# env / secrets (server-only, never committed)
.env
.env.*
!.env.example

# misc
.DS_Store
*.pem
npm-debug.log*

# local backups / dumps
/backups
